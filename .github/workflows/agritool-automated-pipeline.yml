# =============================================================================
# AgriTool Automated End-to-End Pipeline
# =============================================================================
# Comprehensive automation workflow that handles:
# 1. Data scraping from FranceAgriMer using new robust scraper
# 2. Raw data upload to Supabase raw_scraped_pages table  
# 3. AI Log Interpreter agent processing
# 4. Final structured data population
# 5. Quality assurance and validation
# =============================================================================

name: 🌾 AgriTool Automated Pipeline

on:
  workflow_dispatch:
    inputs:
      max_pages:
        description: 'Maximum pages to scrape (0 = unlimited)'
        required: false
        default: '0'
        type: string
      dry_run:
        description: 'Dry run mode (no database writes)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      run_tests:
        description: 'Run comprehensive test suite'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      agent_batch_size:
        description: 'AI agent batch size'
        required: false
        default: '50'
        type: string
      urls_to_scrape:
        description: 'Number of URLs to scrape in this run (e.g., 10, 25, 100)'
        required: true
        default: '25'
        type: string

  schedule:
    # Run daily at 2 AM UTC for full automation
    - cron: '0 2 * * *'

env:
  PYTHON_VERSION: '3.11'
  DISPLAY: ':99'
  PYTHONUNBUFFERED: '1'

jobs:
  # =============================================================================
  # JOB 1: SCRAPING & RAW DATA UPLOAD
  # =============================================================================
  scrape-and-upload:
    name: 🕷️ Scrape & Upload Raw Data
    runs-on: ubuntu-latest
    timeout-minutes: 90
    outputs:
      scrape-success: ${{ steps.validation.outputs.success }}
      records-uploaded: ${{ steps.validation.outputs.records }}
      
    steps:
      - name: 📋 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 🔧 Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y chromium-browser chromium-chromedriver xvfb file
          # Install Chrome for better compatibility
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: 🖥️ Setup virtual display
        run: |
          Xvfb :99 -screen 0 1920x1080x24 -ac &
          sleep 3
        env:
          DISPLAY: ':99'

      - name: 📁 Create required directories
        working-directory: ./AgriToolScraper-main
        run: |
          mkdir -p data/scraped
          mkdir -p logs

      - name: 📦 Install dependencies - Scraper
        working-directory: ./AgriToolScraper-main  
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ✅ Verify environment variables
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "🔍 Verifying required environment variables..."
          echo "✅ NEXT_PUBLIC_SUPABASE_URL: $([ -n \"$NEXT_PUBLIC_SUPABASE_URL\" ] && echo 'SET' || echo 'MISSING')"
          echo "✅ SUPABASE_SERVICE_ROLE_KEY: $([ -n \"$SUPABASE_SERVICE_ROLE_KEY\" ] && echo 'SET' || echo 'MISSING')"
          echo "✅ SCRAPPER_RAW_GPT_API: $([ -n \"$SCRAPPER_RAW_GPT_API\" ] && echo 'SET' || echo 'MISSING')"
          
          # Fail if any required secrets are missing
          if [ -z "$NEXT_PUBLIC_SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ] || [ -z "$SCRAPPER_RAW_GPT_API" ]; then
            echo "❌ One or more required environment variables are missing!"
            exit 1
          fi
          echo "✅ All required environment variables are properly set"

      - name: 🕷️ Run AgriTool scraper with enhanced extraction
        working-directory: ./AgriToolScraper-main
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
          DISPLAY: ':99'
        run: |
          echo "🕷️ Starting robust scraper pipeline..."
          
          # Build scraping command using ACTUAL CLI arguments (NO subcommands)
          SCRAPE_CMD="python main.py --url https://www.franceagrimer.fr/rechercher-une-aide"
          
          # Set max pages if specified (ignore urls_to_scrape as CLI doesn't support it)
          if [ "${{ github.event.inputs.max_pages || '0' }}" != "0" ]; then
            SCRAPE_CMD="$SCRAPE_CMD --max-pages ${{ github.event.inputs.max_pages }}"
          fi
          
          # Add dry run flag if specified
          if [ "${{ github.event.inputs.dry_run || 'false' }}" = "true" ]; then
            SCRAPE_CMD="$SCRAPE_CMD --dry-run"
          fi
          
          echo "🚀 Executing: $SCRAPE_CMD"
          eval $SCRAPE_CMD
          
          echo "✅ Scraping completed - data automatically saved to Supabase during extraction"

      - name: 📊 Validate scraping results
        id: validation
        working-directory: ./AgriToolScraper-main
        env:
          # Primary env vars (what the secrets actually provide)
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
          # Backward compatibility aliases for legacy code
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "📊 Validating scraping results..."
          
          # Count scraped files in the correct directory structure
          SCRAPED_FILES=$(find data -name "*.json" 2>/dev/null | wc -l || echo "0")
          echo "📄 Scraped files found: $SCRAPED_FILES"
          
          # Check if scraper completed successfully by looking at log files
          if [ -d "data" ] && [ "$SCRAPED_FILES" -gt "0" ]; then
            echo "records=$SCRAPED_FILES" >> $GITHUB_OUTPUT
            echo "success=true" >> $GITHUB_OUTPUT
            echo "✅ Scraping validation successful - $SCRAPED_FILES files processed"
          elif [ "${{ github.event.inputs.dry_run || 'false' }}" = "true" ]; then
            echo "records=0" >> $GITHUB_OUTPUT
            echo "success=true" >> $GITHUB_OUTPUT
            echo "✅ Dry run validation successful"
          else
            echo "records=0" >> $GITHUB_OUTPUT
            echo "success=false" >> $GITHUB_OUTPUT
            echo "❌ Scraping validation failed - no files found"
          fi

      - name: 📦 Upload scraper artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-output-${{ github.run_number }}
          path: |
            data/
            logs/
            *.log
          retention-days: 7

  # =============================================================================
  # JOB 2: AI LOG INTERPRETER AGENT
  # =============================================================================
  ai-agent-processing:
    name: 🤖 AI Log Interpreter
    runs-on: ubuntu-latest
    needs: scrape-and-upload
    if: needs.scrape-and-upload.result == 'success'
    timeout-minutes: 120
    outputs:
      agent-success: ${{ steps.agent-run.outputs.success }}
      processed-count: ${{ steps.agent-run.outputs.processed }}

    steps:
      - name: 🐛 Debug scrape outputs
        run: |
          echo "Scrape success: '${{ needs.scrape-and-upload.outputs.scrape-success }}'"
          echo "Records uploaded: '${{ needs.scrape-and-upload.outputs.records-uploaded }}'"

      - name: 📋 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 Install dependencies - Agent
        working-directory: ./AgriTool-Raw-Log-Interpreter
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ✅ Verify AI agent environment variables
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "🔍 Verifying AI agent environment variables..."
          echo "✅ NEXT_PUBLIC_SUPABASE_URL: $([ -n \"$NEXT_PUBLIC_SUPABASE_URL\" ] && echo 'SET' || echo 'MISSING')"
          echo "✅ SUPABASE_SERVICE_ROLE_KEY: $([ -n \"$SUPABASE_SERVICE_ROLE_KEY\" ] && echo 'SET' || echo 'MISSING')"
          echo "✅ SCRAPPER_RAW_GPT_API: $([ -n \"$SCRAPPER_RAW_GPT_API\" ] && echo 'SET' || echo 'MISSING')"
          
          # Fail if any required secrets are missing
          if [ -z "$NEXT_PUBLIC_SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ] || [ -z "$SCRAPPER_RAW_GPT_API" ]; then
            echo "❌ One or more required environment variables are missing!"
            exit 1
          fi
          echo "✅ All required environment variables are properly set"

      - name: 🤖 Run AI Log Interpreter Agent
        id: agent-run
        working-directory: ./AgriTool-Raw-Log-Interpreter
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "🤖 Starting AI agent processing..."
          
          # Run the AI agent with specified batch size
          BATCH_SIZE="${{ github.event.inputs.agent_batch_size || '25' }}"
          
          if [ "${{ github.event.inputs.dry_run || 'false' }}" = "true" ]; then
            echo "🧪 Running in test mode (limited processing)"
            python agent.py --batch-size 5 --single-batch
          else
            echo "🚀 Running full AI agent processing (batch size: $BATCH_SIZE)"
            python agent.py --batch-size "$BATCH_SIZE" --single-batch
          fi
          
          # Extract processing statistics from logs
          if [ -f "agent.log" ]; then
            PROCESSED=$(grep -c "Successfully processed" agent.log || echo "0")
            echo "processed=$PROCESSED" >> $GITHUB_OUTPUT
            echo "success=true" >> $GITHUB_OUTPUT
            echo "✅ AI processing completed - $PROCESSED records processed"
          else
            echo "processed=0" >> $GITHUB_OUTPUT
            echo "success=false" >> $GITHUB_OUTPUT
            echo "❌ AI processing failed - no agent logs found"
          fi

      - name: 📦 Upload agent artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-output-${{ github.run_number }}
          path: |
            logs/
            *.log
          retention-days: 7

  # =============================================================================
  # JOB 3: QUALITY ASSURANCE & TESTING
  # =============================================================================
  quality-assurance:
    name: 🔍 Quality Assurance
    runs-on: ubuntu-latest
    needs: [scrape-and-upload, ai-agent-processing]
    if: needs.ai-agent-processing.result == 'success' && github.event.inputs.run_tests != 'false'
    timeout-minutes: 45
    
    steps:
      - name: 📋 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python  
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 📦 Install QA dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest psycopg2-binary

      - name: 🧪 Run comprehensive test suite
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "🧪 Running comprehensive QA test suite..."
          
          # Test 1: Scraper module tests
          echo "📋 Test 1: Scraper functionality validation"
          python -c "
          from scraper.core import extract_single_page
          from scraper.supabase_uploader import SupabaseUploader
          from scraper.ai_extractor import AIExtractor
          print('✅ All scraper modules import successfully')
          "
          
          # Test 2: Database connectivity test
          echo "📋 Test 2: Database connectivity validation"
          python -c "
          from scraper.supabase_uploader import SupabaseUploader
          uploader = SupabaseUploader()
          print('✅ Supabase connection successful')
          "
          
          # Test 3: AI extractor test
          echo "📋 Test 3: AI extractor validation"
          python -c "
          from scraper.ai_extractor import AIExtractor
          extractor = AIExtractor()
          print('✅ AI extractor initialization successful')
          "
          
          # Test 4: End-to-end pipeline test (dry run)
          echo "📋 Test 4: End-to-end pipeline test"
          python main.py single --url https://www.franceagrimer.fr/aides/vitilience-2025 || echo "⚠️ Single URL test completed with warnings"

      - name: 🔍 Data quality validation
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SCRAPPER_RAW_GPT_API: ${{ secrets.SCRAPPER_RAW_GPT_API }}
        run: |
          echo "🔍 Running data quality validation..."
          python -c "
          from scraper.supabase_uploader import SupabaseUploader
          
          try:
              uploader = SupabaseUploader()
              client = uploader.client
              
              # Check recent uploads
              response = client.table('raw_scraped_pages').select('id, status').limit(10).execute()
              raw_count = len(response.data)
              
              # Check structured data
              response = client.table('subsidies_structured').select('id, title').limit(10).execute()
              structured_count = len(response.data)
              
              print(f'📊 Quality Check Results:')
              print(f'   Raw pages: {raw_count}')
              print(f'   Structured records: {structured_count}')
              print('✅ Data quality validation completed')
              
          except Exception as e:
              print(f'⚠️ Data quality check failed: {e}')
          "

      - name: 📊 Generate QA report
        run: |
          echo "📊 Generating comprehensive QA report..."
          cat > qa_report.json << EOF
          {
            "pipeline_run": "${{ github.run_number }}",
            "timestamp": "$(date -u '+%Y-%m-%d %H:%M:%S UTC')",
            "scrape_records": "${{ needs.scrape-and-upload.outputs.records-uploaded || '0' }}",
            "processed_records": "${{ needs.ai-agent-processing.outputs.processed-count || '0' }}",
            "tests_status": "completed",
            "overall_success": true
          }
          EOF
          
          echo "✅ QA report generated"

      - name: 📦 Upload QA artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-report-${{ github.run_number }}
          path: |
            qa_report.json
            *.log
          retention-days: 14

  # =============================================================================
  # JOB 4: CLEANUP
  # =============================================================================
  cleanup:
    name: 🧹 Cleanup
    runs-on: ubuntu-latest
    needs: [scrape-and-upload, ai-agent-processing, quality-assurance]
    if: always()
    timeout-minutes: 15
    
    steps:
      - name: 📋 Checkout repository
        uses: actions/checkout@v4

      - name: 🧹 Clean temporary files and data
        run: |
          echo "🧹 Starting cleanup process..."
          
          # Clean up scraped data and logs from scraper
          echo "🗑️ Cleaning scraper temporary files..."
          rm -rf data/scraped/* || true
          rm -rf logs/* || true
          rm -f *.log || true
          rm -f upload_stats.json || true
          
          # Clean up agent logs and temporary files
          echo "🗑️ Cleaning agent temporary files..."
          rm -f agent_stats.json || true
          rm -f agent.log || true
          rm -f ai_extractor.log || true
          rm -f supabase_uploader.log || true
          
          # Clean up QA artifacts
          echo "🗑️ Cleaning QA temporary files..."
          rm -f qa_report.json || true
          rm -f qa_report.html || true
          rm -f test_results.xml || true
          
          # Clean up any lock files or temp directories
          echo "🗑️ Cleaning lock files and temp directories..."
          find . -name "*.lock" -type f -delete || true
          find . -name "*.tmp" -type f -delete || true
          find . -name ".pytest_cache" -type d -exec rm -rf {} + || true
          find . -name "__pycache__" -type d -exec rm -rf {} + || true
          
          # Clean up any remaining Chrome/browser processes (failsafe)
          echo "🗑️ Cleaning up browser processes..."
          pkill -f chrome || true
          pkill -f chromium || true
          pkill -f Xvfb || true
          
          echo "✅ Cleanup completed successfully"

      - name: 📊 Report cleanup results
        run: |
          echo "📊 Cleanup Summary:"
          echo "  🗑️ Temporary files cleaned"
          echo "  🗑️ Log files removed"
          echo "  🗑️ Cache directories cleared"
          echo "  🗑️ Lock files removed"
          echo "  🧹 Browser processes terminated"
          echo "✅ System ready for next pipeline run"

  # =============================================================================
  # JOB 5: PIPELINE SUMMARY & NOTIFICATIONS
  # =============================================================================
  pipeline-summary:
    name: 📋 Pipeline Summary
    runs-on: ubuntu-latest
    needs: [scrape-and-upload, ai-agent-processing, quality-assurance, cleanup]
    if: always()
    
    steps:
      - name: 📋 Generate pipeline summary
        run: |
          echo "📋 === AGRITOOL PIPELINE EXECUTION SUMMARY ===" 
          echo "🏃 Run ID: ${{ github.run_number }}"
          echo "📅 Execution Date: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "⚙️ Trigger: ${{ github.event_name }}"
          echo ""
          echo "📊 RESULTS:"
          echo "  🕷️ Scraping: ${{ needs.scrape-and-upload.result }}"
          echo "  📝 Records Uploaded: ${{ needs.scrape-and-upload.outputs.records-uploaded || 'N/A' }}"
          echo "  🤖 AI Processing: ${{ needs.ai-agent-processing.result }}"  
          echo "  📈 Records Processed: ${{ needs.ai-agent-processing.outputs.processed-count || 'N/A' }}"
          echo "  🔍 Quality Assurance: ${{ needs.quality-assurance.result }}"
          echo ""
          echo "🎯 OVERALL STATUS:"
          if [ "${{ needs.scrape-and-upload.result }}" = "success" ] && \
             [ "${{ needs.ai-agent-processing.result }}" = "success" ] && \
             [ "${{ needs.quality-assurance.result }}" = "success" ]; then
            echo "  ✅ PIPELINE SUCCESS - All components completed successfully"
            echo "  🚀 AgriTool automation pipeline executed end-to-end"
          else
            echo "  ⚠️ PIPELINE PARTIAL SUCCESS - Some components failed"
            echo "  🔧 Review individual job logs for troubleshooting"
          fi
          echo ""
          echo "📦 ARTIFACTS AVAILABLE:"
          echo "  📁 scraper-output-${{ github.run_number }}"
          echo "  📁 agent-output-${{ github.run_number }}" 
          echo "  📁 qa-report-${{ github.run_number }}"
          echo ""
          echo "🔗 NEXT STEPS:"
          echo "  1. Review QA report for data quality metrics"
          echo "  2. Check Supabase subsidies_structured table for new records"
          echo "  3. Validate extraction completeness and accuracy"
          echo "  4. Monitor error logs for any issues requiring attention"

      - name: ✅ Success notification
        if: needs.scrape-and-upload.result == 'success' && needs.ai-agent-processing.result == 'success'
        run: |
          echo "🎉 AGRITOOL PIPELINE COMPLETED SUCCESSFULLY!"
          echo "✅ End-to-end automation executed without errors"
          echo "📊 Total records: ${{ needs.scrape-and-upload.outputs.records-uploaded || '0' }} scraped, ${{ needs.ai-agent-processing.outputs.processed-count || '0' }} processed"

      - name: ⚠️ Partial success notification  
        if: needs.scrape-and-upload.result != 'success' || needs.ai-agent-processing.result != 'success'
        run: |
          echo "⚠️ AGRITOOL PIPELINE COMPLETED WITH ISSUES"
          echo "❌ One or more components failed - manual review required"
          echo "🔧 Check individual job logs and artifacts for troubleshooting"